---
phase: 05-5-classification-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/gcn/feature_extraction.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "calculate_angle() function uses 3D coordinates instead of 2D"
    - "All angle calculations compute 3D angles between vectors"
    - "Thrust vs block classification can use depth (z-axis) discrimination"
  artifacts:
    - path: "app/models/gcn/feature_extraction.py"
      provides: "3D angle calculation function"
      min_changes: 5
      contains: "def calculate_angle(p1, p2, p3):"
  key_links:
    - from: "calculate_angle()"
      to: "compute_global_features_from_kpts()"
      via: "function call"
      pattern: "features\['.*_angle'\] = calculate_angle"
---

<objective>
Implement 3D angle calculation in feature_extraction.py to distinguish thrust vs block techniques using depth information (z-axis).

Purpose: Thrusts extend forward (large z variation) while blocks move sideways (minimal z variation). In 2D projection from front view, these look identical. 3D angles will better distinguish them.

Output: Modified calculate_angle() function using 3D coordinates [x, y, z].
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-5-classification-optimization/05-5-CONTEXT.md
@app/models/gcn/feature_extraction.py

## User Decision (D1 - LOCKED)

**Decision:** Upgrade `calculate_angle()` to use 3D coordinates [x, y, z]

**Implementation:**
- Modify `calculate_angle(p1, p2, p3)` in `feature_extraction.py`
- Use 3D vectors and proper 3D angle calculation
- Keep feature count the same (don't add 2D+3D, replace 2D with 3D)

## Current 2D Implementation (lines 12-19)

```python
def calculate_angle(p1, p2, p3):
    """Calculate angle at p2 formed by p1-p2-p3"""
    v1 = np.array([p1[0] - p2[0], p1[1] - p2[1]])
    v2 = np.array([p3[0] - p2[0], p3[1] - p2[1]])
    
    cos_angle = np.dot(v1, v2) / (np.linalg.norm(v1) * np.linalg.norm(v2) + 1e-6)
    angle = np.arccos(np.clip(cos_angle, -1.0, 1.0))
    return np.degrees(angle)
```

## 3D Angle Calculation Reference

For 3D points p1=[x1,y1,z1], p2=[x2,y2,z2], p3=[x3,y3,z3]:
- v1 = [x1-x2, y1-y2, z1-z2]
- v2 = [x3-x2, y3-y2, z3-z2]
- cos_angle = dot(v1, v2) / (|v1| * |v2|)
- angle = arccos(cos_angle) in degrees

## Functions Using calculate_angle

From compute_global_features_from_kpts():
- `features['left_elbow_angle']` = calculate_angle(kpts[11], kpts[13], kpts[15])
- `features['right_elbow_angle']` = calculate_angle(kpts[12], kpts[14], kpts[16])
- `features['left_shoulder_angle']` = calculate_angle(kpts[13], kpts[11], kpts[23])
- `features['right_shoulder_angle']` = calculate_angle(kpts[14], kpts[12], kpts[24])
- `features['left_knee_angle']` = calculate_angle(kpts[23], kpts[25], kpts[27])
- `features['right_knee_angle']` = calculate_angle(kpts[24], kpts[26], kpts[28])

MediaPipe kpts format: [33, 4] array of [x, y, z, visibility]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update calculate_angle() to 3D</name>
  <files>app/models/gcn/feature_extraction.py</files>
  <read_first>
    - app/models/gcn/feature_extraction.py (read the calculate_angle function and its usage)
  </read_first>
  <action>
    Modify the `calculate_angle(p1, p2, p3)` function to use 3D coordinates:
    
    1. Change vector construction from 2D to 3D:
       - OLD: `v1 = np.array([p1[0] - p2[0], p1[1] - p2[1]])`
       - NEW: `v1 = np.array([p1[0] - p2[0], p1[1] - p2[1], p1[2] - p2[2]])`
       - Same for v2 using p3 instead of p1
    
    2. Keep the same arccos/dot-product formula (it works for 3D)
    
    3. Update docstring: "Calculate 3D angle at p2 formed by p1-p2-p3 using 3D coordinates"
    
    4. Verify: The 6 angle calculations in compute_global_features_from_kpts() pass kpts[N] which are [x, y, z, vis] arrays - they will automatically use the first 3 elements (x, y, z)
    
    CRITICAL: Do NOT change function signature or return format. Keep backward compatible.
  </action>
  <acceptance_criteria>
    - `grep -A 8 "def calculate_angle" app/models/gcn/feature_extraction.py` shows 3D vector construction with z component
    - File contains `p1\[2\] - p2\[2\]` pattern (z-difference calculation)
    - Docstring mentions "3D" or "three-dimensional"
  </acceptance_criteria>
  <verify>
    <automated>python -c "import app.models.gcn.feature_extraction as fe; import numpy as np; p1, p2, p3 = [0,0,1], [0,0,0], [1,0,0]; angle = fe.calculate_angle(p1, p2, p3); print(f'90-degree 3D angle: {angle:.1f}'); assert 89 < angle < 91, f'Expected ~90, got {angle}'"</automated>
  </verify>
  <done>
    - calculate_angle() uses 3D vectors [dx, dy, dz]
    - All 6 joint angles (elbow, shoulder, knee) compute 3D angles
    - Unit test passes for known 3D angles
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify backward compatibility</name>
  <files>app/models/gcn/feature_extraction.py</files>
  <read_first>
    - app/models/gcn/feature_extraction.py (verify compute_global_features_from_kpts calls)
  </read_first>
  <action>
    Verify that compute_global_features_from_kpts() passes the correct format to calculate_angle():
    
    1. Check that kpts[N] arrays have at least 3 elements (x, y, z)
    2. Confirm the function calls like `calculate_angle(kpts[11], kpts[13], kpts[15])` will work with 3D calculation
    3. kpts from MediaPipe has format [x, y, z, visibility] - indices 0, 1, 2 are the 3D coordinates
    
    If any issues found (e.g., some code passes only 2D arrays), add a compatibility check in calculate_angle():
    ```python
    # Handle 2D input for backward compatibility
    if len(p1) == 2:
        p1 = [p1[0], p1[1], 0.0]
    # etc for p2, p3
    ```
    
    However, based on current code review, MediaPipe always returns [x, y, z, visibility], so this shouldn't be needed.
  </action>
  <acceptance_criteria>
    - All 6 calculate_angle() calls in compute_global_features_from_kpts() pass arrays with at least 3 elements
    - No runtime errors when extracting features from test pose
  </acceptance_criteria>
  <verify>
    <automated>python -c "import numpy as np; from app.models.gcn.feature_extraction import compute_global_features_from_kpts; kpts = np.random.rand(33, 4); stick = np.array([[0.5, 0.5, 0, 1], [0.6, 0.6, 0, 1]]); features = compute_global_features_from_kpts(kpts, stick); print(f'Extracted {len(features)} features'); print(f'Left elbow angle: {features.get(\"left_elbow_angle\", \"MISSING\")}')"</automated>
  </verify>
  <done>
    - Feature extraction works with 3D angles
    - No compatibility issues detected
    - All 6 joint angles successfully computed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pose_keypoints input | MediaPipe output crossing into feature extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Input keypoints | accept | MediaPipe is trusted source; no integrity check needed for CV pipeline |
| T-05-01-02 | Denial of Service | NaN in z-coordinate | mitigate | np.arccos handles edge cases; add clipping for safety |
</threat_model>

<verification>
Overall verification:
- 3D angle calculation implemented correctly
- All joint angles (6) use 3D coordinates
- Feature extraction pipeline functions normally
- Thrust vs block can now discriminate using z-depth
</verification>

<success_criteria>
- calculate_angle() computes 3D angles using [x, y, z] coordinates
- All 6 joint angle features (elbow, shoulder, knee - left/right) use 3D calculation
- Feature extraction continues to work with existing code
- No breaking changes to function signatures
</success_criteria>

<output>
After completion, create `.planning/phases/05-5-classification-optimization/05-01-SUMMARY.md`
</output>
