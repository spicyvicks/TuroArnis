---
phase: 06-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TuroArnis.spec
  - scripts/build_app.py
  - app/utils/resource_path.py
  - .planning/phases/06-packaging/06-01-PLAN.md
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
user_setup: []
must_haves:
  truths:
    - PyInstaller spec file produces working executable without errors
    - All ML models (.pth, .pt files) are bundled in _internal folder
    - All instructional GIFs (36 total) are accessible at runtime
    - UI assets (icons, images) are bundled correctly
    - Hidden imports for sklearn, ultralytics, mediapipe, torch_scatter, torch_sparse are resolved
    - Resource path resolution works in both dev and packaged modes
  artifacts:
    - path: TuroArnis.spec
      provides: PyInstaller specification with complete configuration
      must_contain:
        - hiddenimports for all ML libraries
        - datas for app/assets, lesson/*_gif, app/models
        - torch_geometric workaround
        - excludes for torch_geometric.distributed
    - path: app/utils/resource_path.py
      provides: Runtime path resolution utility
      exports: [get_resource_path, get_app_data_path]
    - path: scripts/build_app.py
      provides: Automated build script with validation
  key_links:
    - from: TuroArnis.spec
      to: app/utils/resource_path.py
      via: datas paths match resource_path expectations
      pattern: "app/models" in both spec and resource_path
---

<objective>
Validate and optimize the existing PyInstaller build configuration. Ensure the spec file correctly bundles all assets (models, GIFs, UI resources) and resolves hidden imports for ML libraries. Verify resource path resolution works seamlessly in both development and packaged modes.

Purpose: Establish a reliable, reproducible build system that packages the TuroArnis CV/ML application into a standalone Windows executable.
Output: Validated spec file, working build automation, and resource path utilities.
</objective>

<execution_context>
@C:\Users\HP\Documents\GitHub\TuroArnis\.planning\phases\06-packaging\06-01-PLAN.md
</execution_context>

<context>
@C:\Users\HP\Documents\GitHub\TuroArnis\TuroArnis.spec
@C:\Users\HP\Documents\GitHub\TuroArnis\app\utils\resource_path.py
@C:\Users\HP\Documents\GitHub\TuroArnis\scripts\build_app.py
@C:\Users\HP\Documents\GitHub\TuroArnis\requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and Optimize Spec File Configuration</name>
  <files>TuroArnis.spec</files>
  <action>
Review TuroArnis.spec for completeness and correctness per requirements PKG-01, PKG-02, PKG-03:

1. Verify hiddenimports coverage for all ML libraries:
   - sklearn submodules (already listed)
   - ultralytics (already listed)
   - mediapipe (already listed)
   - torch_scatter, torch_sparse (collected via collect_all())
   - scipy special functions (already listed)

2. Verify asset bundling (datas array):
   - app/assets/ folder (icons, TA.ico)
   - lesson/front_gif/, lesson/left_gif/, lesson/right_gif/ (36 total GIFs)
   - app/models/ folder (GCN .pth files, config JSON)
   - yolov8n.pt (root level YOLO model)

3. Verify torch_geometric workaround:
   - torch_geometric path added to datas
   - torch_geometric.distributed excluded from hiddenimports
   - torch_geometric.distributed in Analysis excludes list

4. Verify build settings per locked decisions:
   - upx=True (D-05: UPX compression enabled)
   - console=True (D-04: Console enabled for debugging)
   - exclude_binaries=True (D-02: One-directory mode)
   - icon='app/assets/TA.ico' (D-06: Application icon)

5. Run dry-run validation: `python scripts/build_app.py --dry-run`
   - Note any missing imports or asset warnings
   - Fix any issues by adding to hiddenimports or datas

Document any changes made in the spec file comments.
  </action>
  <verify>
    <automated>python scripts/build_app.py --dry-run</automated>
    <expect>All pre-flight checks PASS</expect>
  </verify>
  <done>
    - TuroArnis.spec passes dry-run validation
    - No missing model files reported
    - No missing GIF directories reported
    - PyInstaller version matches requirements.txt (6.12.0)
    - All hiddenimports resolve without warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate Resource Path Resolution</name>
  <files>app/utils/resource_path.py, scripts/test_resource_paths.py</files>
  <action>
Ensure resource path resolution works correctly in both dev and packaged modes per PKG-04:

1. Review app/utils/resource_path.py implementation:
   - get_resource_path() handles sys._MEIPASS for PyInstaller
   - get_resource_path() handles dev mode (project root resolution)
   - get_app_data_path() creates user data directory properly

2. Create comprehensive test script scripts/test_resource_paths.py:
   ```python
   import sys
   import os
   sys.path.insert(0, os.path.abspath('.'))
   
   from app.utils.resource_path import get_resource_path, get_app_data_path
   
   # Test paths that should work in both dev and packaged modes
   test_paths = [
       'app/models/hybrid_gcn_v2_front.pth',
       'app/models/gcn_model_config.json', 
       'app/assets/TA.ico',
       'yolov8n.pt',
       'lesson/front_gif/crown.gif',
   ]
   
   print("Testing resource path resolution (DEV MODE):\n")
   for path in test_paths:
       full_path = get_resource_path(path)
       exists = os.path.exists(full_path)
       print(f"  {'[OK]' if exists else [MISSING]} {path}")
       print(f"       -> {full_path}\n")
   
   # Test app data path
   app_data = get_app_data_path()
   print(f"\nApp data path: {app_data}")
   print(f"  Exists: {os.path.exists(app_data)}")
   ```

3. Run the test in development mode:
   ```bash
   python scripts/test_resource_paths.py
   ```

4. Verify all test paths resolve correctly and files exist.

5. Document the test results in .planning/phases/06-packaging/resource_path_test_results.md
  </action>
  <verify>
    <automated>python scripts/test_resource_paths.py</automated>
    <expect>All test paths resolve to existing files</expect>
  </verify>
  <done>
    - scripts/test_resource_paths.py created and runs successfully
    - All 5 critical resource paths resolve correctly
    - All referenced files exist at resolved paths
    - get_app_data_path() returns valid, writable directory
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute Clean Build and Verify Output</name>
  <files>build.log, dist/TuroArnis/</files>
  <action>
Execute a clean build using the build automation script per PKG-01, PKG-06:

1. Run clean build:
   ```bash
   python scripts/build_app.py --clean
   ```

2. Monitor build progress and capture output to build.log

3. Verify build completes without errors (exit code 0)

4. Verify build output structure:
   - dist/TuroArnis/TuroArnis.exe exists (should be ~3-10MB)
   - dist/TuroArnis/_internal/ folder exists with all dependencies
   - dist/TuroArnis/_internal/app/models/ contains .pth files
   - dist/TuroArnis/_internal/lesson/*/ contains GIF files
   - dist/TuroArnis/_internal/app/assets/ contains icons

5. Verify _internal folder contains key ML libraries:
   - torch/ folder with PyTorch libraries
   - mediapipe/ folder
   - ultralytics/ folder
   - scipy/ folder
   - sklearn/ folder

6. Calculate and log total distribution size (target: < 500MB ideally, report actual)

7. Document any build warnings or issues in build_analysis.md
  </action>
  <verify>
    <automated>python scripts/build_app.py --clean</automated>
    <expect>Exit code 0, executable created, _internal folder populated</expect>
  </verify>
  <done>
    - Clean build completes successfully (exit code 0)
    - TuroArnis.exe exists in dist/TuroArnis/
    - _internal/ folder contains >1000 files
    - Model files (.pth) present in _internal/app/models/
    - GIF files present in _internal/lesson/*/ directories
    - Total distribution size documented
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Build system → Executable | Build artifacts could be corrupted or incomplete |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | Build process | accept | Build failures are caught by exit codes and logging |
| T-06-02 | Information Disclosure | build.log | accept | Build logs contain paths but no secrets; console enabled for debugging per D-04 |
| T-06-03 | Tampering | Spec file | mitigate | Version control tracks all spec changes; validate before each build |
</threat_model>

<verification>
After completion, verify:
1. `python scripts/build_app.py --dry-run` passes all checks
2. `python scripts/test_resource_paths.py` confirms all paths resolve
3. `dir dist\TuroArnis\TuroArnis.exe` exists
4. `dir dist\TuroArnis\_internal\app\models` contains .pth files
5. Build log shows no critical errors
</verification>

<success_criteria>
- PyInstaller spec file validates without warnings on dry-run
- Resource path tests pass for all critical assets (models, GIFs, icons)
- Clean build produces working executable (dist/TuroArnis/TuroArnis.exe)
- All ML model files are bundled in _internal folder
- All instructional GIFs are accessible in packaged app
- Total build output is documented and within reasonable size (< 1GB)
</success_criteria>

<output>
After completion, create `.planning/phases/06-packaging/06-01-SUMMARY.md` documenting:
- Spec file audit results
- Resource path test results
- Build output statistics (size, file counts)
- Any issues encountered and resolutions
- Next: Plan 02 (Executable Testing) is unblocked
</output>
